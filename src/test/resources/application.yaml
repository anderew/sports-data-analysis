logging:
   level:
     org.springframework.web: DEBUG
     org.rendell: DEBUG
spring:
  logging:
    level:
      org.springframework.web: DEBUG
      org.rendell: DEBUG
strava:
  authorisation: fake_toekn
google:
  key: fake_key